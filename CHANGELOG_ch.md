## 0.8.2 (2026-02-25)

- 修复并行 HNSW 索引构建时的缓冲区溢出问题 - [详情](https://github.com/pgvector/pgvector/issues/959)
- 改进 Windows 平台上的 `install` 目标
- 修复 Postgres 18 中 `EXPLAIN` 输出里 `Index Searches` 的显示问题

## 0.8.1 (2025-09-04)

- 新增对 Postgres 18 rc1 的支持
- 提升 `binary_quantize` 函数的性能

## 0.8.0 (2024-10-30)

- 新增对迭代式索引扫描的支持
- 新增数组到 `sparsevec` 的类型转换
- 改进代价估算以在过滤场景下更好地选择索引
- 提升 HNSW 索引扫描的性能
- 提升 HNSW 插入及磁盘索引构建的性能
- 停止对 Postgres 12 的支持

## 0.7.4 (2024-08-05)

- 修复并行 HNSW 索引构建时的锁定问题
- 修复在未启用 SSE2 的 i386 架构上使用 GCC 14 时的编译错误

## 0.7.3 (2024-07-22)

- 修复 `sparsevec` 引发的 `failed to add index item` 错误
- 修复 FreeBSD ARM 平台上的编译错误
- 修复 MSVC 与 Postgres 16 组合下的编译警告

## 0.7.2 (2024-06-11)

- 修复未日志记录表（unlogged table）上索引的初始化分支问题

## 0.7.1 (2024-06-03)

- 提升磁盘 HNSW 索引构建的性能
- 修复 GCC 8 下的 `undefined symbol` 错误
- 修复 Mac 上通用二进制文件的编译错误
- 修复 Clang < 14 下的编译警告

## 0.7.0 (2024-04-29)

- 新增 `halfvec` 类型
- 新增 `sparsevec` 类型
- 新增对 `bit` 类型的索引支持
- 新增 HNSW L1 距离的索引支持
- 新增 `binary_quantize` 函数
- 新增 `hamming_distance` 函数
- 新增 `jaccard_distance` 函数
- 新增 `l2_normalize` 函数
- 新增 `subvector` 函数
- 新增向量拼接运算符
- 新增 Linux x86-64 平台距离函数的 CPU 动态调度
- 更新比较运算符以支持不同维度的向量

## 0.6.2 (2024-03-18)

- 降低并行 HNSW 索引构建时的锁竞争

## 0.6.1 (2024-03-04)

- 修复 `ANALYZE` 在不同维度向量下的报错问题
- 修复 `shared_preload_libraries` 引发的段错误
- 修复向量减法被错误标记为可交换运算的问题

## 0.6.0 (2024-01-29)

如需在 Postgres 12 或 Docker 环境下升级，请参阅[相关说明](https://github.com/pgvector/pgvector#060)。

- 新增 HNSW 并行索引构建支持
- 新增 GUC 参数校验
- 将向量的存储方式从 `extended` 改为 `external`
- 提升 HNSW 性能
- 降低 HNSW 索引构建的内存占用
- 减少 HNSW 索引构建产生的 WAL 日志量
- 修复逻辑复制错误
- 修复 HNSW 索引构建时的 `invalid memory alloc request size` 错误
- 将 Docker 镜像迁移至 `pgvector` 组织
- 为每个受支持的 Postgres 版本新增 Docker 标签
- 停止对 Postgres 11 的支持

## 0.5.1 (2023-10-10)

- 提升 HNSW 索引构建的性能
- 新增索引扫描时对 MVCC 兼容快照的检查

## 0.5.0 (2023-08-28)

- 新增 HNSW 索引类型
- 新增 IVFFlat 并行索引构建支持
- 新增 `l1_distance` 函数
- 新增向量逐元素乘法
- 新增 `sum` 聚合函数
- 提升距离函数的性能
- 修复余弦距离结果越界问题
- 修复 IVFFlat 中 NULL 和 NaN 距离的计算结果

## 0.4.4 (2023-06-12)

- 改进畸形向量字面量的错误提示信息
- 修复文本输入引发的段错误
- 修复文本输入中连续分隔符的处理问题

## 0.4.3 (2023-06-10)

- 改进代价估算
- 改进文本输入中空格的处理
- 修复二进制输入中的无穷大和 NaN 值问题
- 修复向量加减法中的无穷大值问题
- 修复列表中心点中的无穷大值问题
- 修复 `float8` 按引用传递时的编译错误
- 修复 PowerPC 平台上的编译错误
- 修复 i386 平台上创建索引时的段错误

## 0.4.2 (2023-05-13)

- 新增数据量不足时创建索引的提示信息
- 修复部分直接函数调用的维度校验问题
- 修复 Postgres 12.0–12.2 上的安装错误

## 0.4.1 (2023-03-21)

- 提升余弦距离的计算性能
- 修复索引扫描计数问题

## 0.4.0 (2023-01-11)

如需在 Postgres < 13 环境下升级，请参阅[相关说明](https://github.com/pgvector/pgvector/blob/v0.4.0/README.md#040)。

- 将向量元素的文本表示方式改为与 `real` 类型一致
- 将向量的存储方式从 `plain` 改为 `extended`
- 将向量的最大维度从 1024 提升至 16000
- 将索引的最大维度从 1024 提升至 2000
- 改进特定输入下文本解析的精度
- 新增向量的 `avg` 聚合函数
- 新增对 Windows 的实验性支持
- 停止对 Postgres 10 的支持

## 0.3.2 (2022-11-22)

- 修复 `invalid memory alloc request size` 错误

## 0.3.1 (2022-11-02)

如果从 0.2.7 或 0.3.0 升级，请在升级后[重建](https://github.com/pgvector/pgvector/blob/v0.3.1/README.md#031)所有 `ivfflat` 索引，以确保所有数据均被索引。

- 修复静默损坏 `ivfflat` 索引的插入问题（该问题于 0.2.7 引入）
- 修复列表数量 > 6500 时创建索引引发的段错误

## 0.3.0 (2022-10-15)

- 新增对 Postgres 15 的支持
- 停止对 Postgres 9.6 的支持

## 0.2.7 (2022-07-31)

- 修复 `unexpected data beyond EOF` 错误

## 0.2.6 (2022-05-22)

- 提升 Postgres < 12 环境下索引创建的性能

## 0.2.5 (2022-02-11)

- 降低索引创建期间的内存占用
- 修复索引创建超出 `maintenance_work_mem` 限制的问题
- 修复列表数量 > 1600 时创建索引的报错问题

## 0.2.4 (2022-02-06)

- 新增并行 vacuum 支持
- 修复索引空间不复用的问题

## 0.2.3 (2022-01-30)

- 新增 Postgres 12+ 的索引构建进度显示
- 改进索引创建期间的中断处理

## 0.2.2 (2022-01-15)

- 修复 Mac ARM 平台上的编译错误

## 0.2.1 (2022-01-02)

- 修复 `operator is not unique` 错误

## 0.2.0 (2021-10-03)

- 新增对 Postgres 14 的支持

## 0.1.8 (2021-09-07)

- 新增 `vector` 到 `real[]` 的类型转换

## 0.1.7 (2021-06-13)

- 新增 `numeric[]` 到 `vector` 的类型转换

## 0.1.6 (2021-06-09)

- 修复 `COUNT` 引发的段错误

## 0.1.5 (2021-05-25)

- 降低索引创建期间的内存占用

## 0.1.4 (2021-05-09)

- 修复内积的 k-means 计算问题
- 修复 GCC 10 下的多重定义错误

## 0.1.3 (2021-05-06)

- 新增 Dockerfile
- 修复版本号

## 0.1.2 (2021-04-26)

- 向量化距离计算
- 改进代价估算

## 0.1.1 (2021-04-25)

- 新增 `COPY` 的二进制表示支持
- 将函数标记为 `PARALLEL SAFE`

## 0.1.0 (2021-04-20)

- 首次发布
